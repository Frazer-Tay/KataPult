// Section timing is handled centrally by AnalyticsTracker so route changes,
// tab backgrounding, and page unloads use one consistent event shape.
const useTimeTracker = () => {};

export default useTimeTracker;
